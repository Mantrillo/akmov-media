/**
 * AKMOV MEDIA — Configuración compartida
 * =======================================
 * URL permanente de la API via Cloudflare Named Tunnel.
 * Esta URL nunca cambia aunque se reinicie el servidor.
 */
const AKMOV_API_BASE = 'https://api.akmovmedia.com';
